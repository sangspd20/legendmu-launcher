// When the DOM is ready, run this function
$(document).ready(function () {
  //Set the carousel options
  $("#featured").carousel({
    pauseOnHover: true,
    interval: 5000,
  });
  $("#galery").carousel({
    pauseOnHover: true,
    interval: 3000,
  });
});
